# task-board プロジェクトルール

## Git 運用ルール

- コードを変更するたびに、変更内容を Git コミットして GitHub にプッシュすること
- コミットメッセージは変更内容を簡潔に日本語で記述すること
- プッシュ先ブランチは原則 `main`（別途指示がある場合はそれに従う）
- `.env` や認証情報を含むファイルは絶対にコミットしないこと

## コーディングルール

- コメントは原則不要。WHY が非自明な場合のみ短く記述する
- 不要な抽象化・将来対応のための設計は行わない
- エラーハンドリングは実際に起こりうる境界値にのみ追加する
- 絵文字はユーザーから明示的に求められた場合のみ使用する

## 🚫 変更禁止

- `refs/` フォルダ内のファイルは読み取り専用。編集・削除・移動禁止
